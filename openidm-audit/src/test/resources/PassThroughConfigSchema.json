{
  "type": "object",
  "id": "/",
  "properties": {
    "message": {
      "type": "string"
    },
    "buffering": {
      "type": "object",
      "id": "urn:jsonschema:org:forgerock:audit:events:handlers:EventHandlerConfiguration:EventBufferingConfiguration",
      "description": "audit.handlers.all.buffering",
      "properties": {
        "autoFlush" : {
          "type": "boolean",
          "description": "audit.handlers.all.buffering.autoFlush"
        },
        "enabled": {
          "type": "boolean",
          "description": "audit.handlers.all.buffering.enabled"
        },
        "maxSize": {
          "type": "integer",
          "description": "audit.handlers.all.buffering.maxSize"
        }
      }
    }
  }
}