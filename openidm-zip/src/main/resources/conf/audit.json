{
    "eventTypes": {
        "activity": {
            "filter": {
                 "actions": [ "create", "update", "delete" ]
            }
        },
        "recon": {
        }
    },
    "logTo": [
        { "logType": "csv", "location": "audit", "recordDelimiter": ";" },
        { "logType": "repository" }
    ]
}
